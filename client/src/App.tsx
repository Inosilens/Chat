import React, {FC} from 'react';

interface IProps {
    id : number,
    name: string
}


export const App : FC = ({}:IProps) => {
    return (
        <div>
        NOW WORK
        </div>
    );
};

